CONFIGURE="./configure
	--prefix=$PREFIX \
	--enable-shared \
	--enable-static \
	--with-freetype-prefix=$PREFIX"
LIBS=$PREFIX/lib/libz.a
SOURCE=SDL_ttf-2.0.9

if [ "$OS" == "Darwin" ]; then
	LIBFIX=libSDL_ttf
fi

# vi: syntax=sh
