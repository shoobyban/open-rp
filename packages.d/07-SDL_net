SOURCE=SDL_net-1.2.7
PATCHES=
TARGET=
CONFIGURE="./configure \
	--prefix=$PREFIX \
	--enable-shared \
	--enable-static"

if [ "$OS" == "Darwin" ]; then
	PATCHES="$PATCHES SDL_net-1.2.7-TCP_NODELAY.patch"
fi

# vi: syntax=sh
