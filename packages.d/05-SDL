SOURCE=SDL-1.2.13
PATCHES=
TARGET=
CONFIGURE="./configure \
	--prefix=$PREFIX \
	--disable-cdrom \
	--enable-shared \
	--enable-static"

if [ "$OS" != "Linux" ]; then
	CONFIGURE="$CONFIGURE \
		--disable-loadso \
		--disable-sdl-dlopen"
fi

# vi: syntax=sh
