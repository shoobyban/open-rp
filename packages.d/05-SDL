CONFIGURE="./configure \
	--prefix=$PREFIX \
	--disable-cdrom \
	--enable-shared \
	--enable-static"
SOURCE=SDL-1.2.14

if [ "$OS" != "Linux" ]; then
	CONFIGURE="$CONFIGURE \
		--disable-loadso \
		--disable-sdl-dlopen"
fi

# vi: syntax=sh
